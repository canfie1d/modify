.app__container {
  // overflow: hidden;
} 

.page-wrapper {
  position: relative;
  height: auto;
  width: 100%;
  padding-top: calc(100vh - 100px);
  box-sizing: content-box;

  .page__content {
    background: $white;
  }
}

.content-flex {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 200px 40px;
}
